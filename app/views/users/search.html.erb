<div class="container">
  <h3>Search Results</h3>
  <% if @results.present? %>
    <%= render partial: 'search_result', locals: {result: @results} %>
  <% else %>
    <h4>Nothing here, chief!</h4>
  <% end %>
</div>